<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:colorPrimary">@color/twitter_primary</item>
        <item name="android:colorAccent">@color/twitter_primary</item>
        <item name="android:colorButtonNormal">@color/twitter_primary</item>
    </style>

</resources>
